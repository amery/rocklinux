# this is to be sourced by bash

INCLUDE() {
	if echo "$1" | grep -q '.*\.set$'
	then
		cat ./misc/pkgsel/sets/$1 | sed 's/#.*//'
	else
		cat $1 | sed 's/#.*//'
	fi
}

ADD() {
	echo "/ $1 / 	{ p; d; };"
}
REMOVE() {
	echo "/ $1 /	{ s/^X /O / ; p; d; };"
}

DISABLE_REST() {
   echo 's/^X /O /;'
}

